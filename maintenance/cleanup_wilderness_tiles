#!/usr/bin/env coffee

db = require '../db'

query =
  terrain: 'wilderness'
  people: {$size: 0}

db.tiles.remove query, (err, tiles) ->
  if err?
    console.log err
  else
    console.log "Removed #{tiles} unused tiles."
  process.exit()
