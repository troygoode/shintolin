.action
  |  You gave #{msg.quantity}x #{data.items[msg.item].name} to
  if msg.building
    |  the #{data.buildings[msg.building].name}. 
  else
    |  <a href='/profile/#{msg.target_slug}'>#{msg.target_name}</a>. 
  span.time= moment(msg.sent).fromNow()
