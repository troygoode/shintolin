.action
  a.neutral(href='/profile/#{msg.sender_slug}')= msg.sender_name
  |  uses #{data.items[msg.item].name} on you, healing #{msg.amount} HP. 
  if msg.remaining > 0
    | #{msg.remaining} HP remains unhealed. 
  else
    | No HP remains unhealed. 
  span.time= moment(msg.sent).fromNow()
