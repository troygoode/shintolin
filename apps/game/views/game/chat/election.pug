.action
  +profileLink(msg.winner_slug, msg.winner_name)
  |  has defeated 
  +profileLink(msg.loser_slug, msg.loser_name)
  |  to become #{msg.leader_title} of 
  +settlementLink(msg)
  | . 
  +timestamp(msg)
