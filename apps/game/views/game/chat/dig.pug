if msg.found
  .action
    | Digging a hole, you find a 
    strong #{data.items[msg.found].name}
    | ! 
    if msg.broken
      | Your cherished #{data.items[msg.broken_item].name} breaks! You throw away the useless pieces in disgust.
    +timestamp(msg)
else
  .action
    | You dig a hole, but find nothing of use. 
    if msg.broken
      | Your cherished #{data.items[msg.broken_item].name} breaks! You throw away the useless pieces in disgust.
    +timestamp(msg)
