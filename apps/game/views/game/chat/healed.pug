.action
  +senderLink(msg)
  |  uses #{data.items[msg.item].name} on you, healing 
  span.heal #{msg.amount} HP
  | . 
  if msg.remaining > 0
    | #{msg.remaining} HP remains unhealed. 
  else
    | No HP remains unhealed. 
  +timestamp(msg)
