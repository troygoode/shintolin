.action
  - var building = data.buildings[msg.building];
  +senderLink(msg)
  if building.text && building.text.repair && building.text.repair.success_nearby
    |  
    span.heal #{building.text.repair.success_nearby}
    | . 
  else
    |  repaired the #{building.name} for 
    span.heal #{msg.gives.tile_hp} HP
    | . 
  +timestamp(msg)
